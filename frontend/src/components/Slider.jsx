import React from 'react';
import '../style/Slider.css'; // Make sure to import your CSS file

const Slider = () => {
  return (
    <section className="animation">
      <div className="center slider">
        {/* You can add content inside the slider if needed */}
      </div>
    </section>
  );
};

export default Slider;
